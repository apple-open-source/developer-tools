--- configure.orig	2018-04-29 22:50:54.000000000 -0700
+++ configure	2018-04-29 22:55:17.000000000 -0700
@@ -24496,8 +24496,8 @@ $as_echo_n "checking perl version... " >
       { $as_echo "$as_me:${as_lineno-$LINENO}: result: $PERL_VERSION" >&5
 $as_echo "$PERL_VERSION" >&6; }
       if test "$PERL_VERSION" -ge "5008000"; then
-        SWIG_PL_INCLUDES="\$(SWIG_INCLUDES) `$PERL -MExtUtils::Embed -e ccopts`"
-        SWIG_PL_LINK="`$PERL -MExtUtils::Embed -e ldopts`"
+        SWIG_PL_INCLUDES="\$(SWIG_INCLUDES) `$PERL -MExtUtils::Embed -e ccopts | sed 's:-arch i386::'`"
+        SWIG_PL_LINK="`$PERL -MExtUtils::Embed -e ldopts | sed 's:-arch i386::'`"
         SWIG_PL_LINK="`
   input_flags="$SWIG_PL_LINK"
   output_flags=""
@@ -25005,8 +25005,8 @@ $as_echo_n "checking perl version... " >
       { $as_echo "$as_me:${as_lineno-$LINENO}: result: $PERL_VERSION" >&5
 $as_echo "$PERL_VERSION" >&6; }
       if test "$PERL_VERSION" -ge "5008000"; then
-        SWIG_PL_INCLUDES="\$(SWIG_INCLUDES) `$PERL -MExtUtils::Embed -e ccopts`"
-        SWIG_PL_LINK="`$PERL -MExtUtils::Embed -e ldopts`"
+        SWIG_PL_INCLUDES="\$(SWIG_INCLUDES) `$PERL -MExtUtils::Embed -e ccopts | sed 's:-arch i386::'`"
+        SWIG_PL_LINK="`$PERL -MExtUtils::Embed -e ldopts | sed 's:-arch i386::'`"
         SWIG_PL_LINK="`
   input_flags="$SWIG_PL_LINK"
   output_flags=""
@@ -25514,8 +25514,8 @@ $as_echo_n "checking perl version... " >
       { $as_echo "$as_me:${as_lineno-$LINENO}: result: $PERL_VERSION" >&5
 $as_echo "$PERL_VERSION" >&6; }
       if test "$PERL_VERSION" -ge "5008000"; then
-        SWIG_PL_INCLUDES="\$(SWIG_INCLUDES) `$PERL -MExtUtils::Embed -e ccopts`"
-        SWIG_PL_LINK="`$PERL -MExtUtils::Embed -e ldopts`"
+        SWIG_PL_INCLUDES="\$(SWIG_INCLUDES) `$PERL -MExtUtils::Embed -e ccopts | sed 's:-arch i386::'`"
+        SWIG_PL_LINK="`$PERL -MExtUtils::Embed -e ldopts | sed 's:-arch i386::'`"
         SWIG_PL_LINK="`
   input_flags="$SWIG_PL_LINK"
   output_flags=""
@@ -26026,8 +26026,8 @@ $as_echo_n "checking perl version... " >
       { $as_echo "$as_me:${as_lineno-$LINENO}: result: $PERL_VERSION" >&5
 $as_echo "$PERL_VERSION" >&6; }
       if test "$PERL_VERSION" -ge "5008000"; then
-        SWIG_PL_INCLUDES="\$(SWIG_INCLUDES) `$PERL -MExtUtils::Embed -e ccopts`"
-        SWIG_PL_LINK="`$PERL -MExtUtils::Embed -e ldopts`"
+        SWIG_PL_INCLUDES="\$(SWIG_INCLUDES) `$PERL -MExtUtils::Embed -e ccopts | sed 's:-arch i386::'`"
+        SWIG_PL_LINK="`$PERL -MExtUtils::Embed -e ldopts | sed 's:-arch i386::'`"
         SWIG_PL_LINK="`
   input_flags="$SWIG_PL_LINK"
   output_flags=""
